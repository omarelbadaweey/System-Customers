<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> Edit Data</title>
  <link rel="icon" href="/img/admin.jpg" />
  <link rel="stylesheet" href="/input.css" />
  <link rel="stylesheet" href="/output.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100">
  <%- include('partials/sideBar') %>

  <div class="bg-white shadow-md rounded-xl p-3 max-w-4xl ml-[25%] mt-28 border border-blue-200">
    <form action="/edit/<%= Edit._id %>?_method=PUT" method="POST" class="space-y-3">
      
      <!-- First & Last Name -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4 capitalize">
        <div>
          <label class="block text-gray-700 font-medium mb-2">First Name</label>
          <input type="text" name="firstname" value="<%= Edit.firstname %>"
            class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-400 focus:outline-none">
        </div>

        <div>
          <label class="block text-gray-700 font-medium mb-2">Last Name</label>
          <input type="text" name="lastname" value="<%= Edit.lastname %>"
            class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-400 focus:outline-none">
        </div>
      </div>

      <!-- Email -->
      <div>
        <label class="block text-gray-700 font-medium mb-2">Email</label>
        <input type="email" name="email" value="<%= Edit.email %>"
          class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-400 focus:outline-none">
      </div>

      <!-- Mobile Number -->
      <div>
        <label class="block text-gray-700 font-medium mb-2">Mobile Number</label>
        <input type="text" name="mobileNumber" value="<%= Edit.mobileNumber %>"
          class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-400 focus:outline-none">
      </div>

      <!-- Age + Gender -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <label class="block text-gray-700 font-medium mb-2">Age</label>
          <input type="number" name="age" min="15" value="<%= Edit.age %>"
            class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-400 focus:outline-none">
        </div>

        <div>
          <label class="block text-gray-700 font-medium mb-2">Gender</label>
          <select name="gender"
            class="w-full border border-gray-300 rounded-lg px-4 py-2 bg-white focus:ring-2 focus:ring-blue-400 focus:outline-none">
            <option hidden >Select Gender</option>
            <option value="male" <%= Edit.gender === 'male' ? 'selected' : '' %>>Male</option>
            <option value="female" <%= Edit.gender === 'female' ? 'selected' : '' %>>Female</option>
          </select>
        </div>
      </div>

      <!-- Country -->
      <div>
        <label class="block text-gray-700 font-medium mb-2">Country</label>
        <select name="country"
          class="w-full border border-gray-300 rounded-lg px-4 py-2 bg-white focus:ring-2 focus:ring-blue-400 focus:outline-none">
          <option hidden >Select Country</option>
          <option value="Egypt" <%= Edit.country === 'Egypt' ? 'selected' : '' %>>Egypt</option>
          <option value="Saudi Arabia" <%= Edit.country === 'Saudi Arabia' ? 'selected' : '' %>>Saudi Arabia</option>
          <option value="UAE" <%= Edit.country === 'UAE' ? 'selected' : '' %>>UAE</option>
          <option value="Kuwait" <%= Edit.country === 'Kuwait' ? 'selected' : '' %>>Kuwait</option>
          <option value="Qatar" <%= Edit.country === 'Qatar' ? 'selected' : '' %>>Qatar</option>
          <option value="Other" <%= Edit.country === 'Other' ? 'selected' : '' %>>Other</option>
        </select>
      </div>

      <!-- Submit -->
      <div class="text-center mt-6">
        <button type="submit"
          class="cursor-pointer bg-blue-400 text-white px-8 py-2 rounded-lg text-lg font-medium hover:bg-blue-500 transition duration-200">
          Update Changes
        </button>
      </div>
    </form>
  </div>
</body>
</html>
